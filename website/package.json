{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Product Landing Page - A modern, responsive static website",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:e2e": "playwright test tests/e2e/",
    "test:unit": "vitest run tests/unit/"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "vite": "^5.2.0",
    "vitest": "^1.4.0"
  }
}
