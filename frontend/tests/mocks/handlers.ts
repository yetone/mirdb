/**
 * MSW Request Handlers
 * Owner: First builder to run
 *
 * Mock Service Worker handlers for API mocking in tests.
 * Note: MSW is not currently installed but handlers are prepared for future use.
 */

export const handlers = [
  // POST /api/urls/ - URL shortening (success case)
  // Placeholder for MSW handlers when needed
]

export const errorHandlers = [
  // Error case handlers
]
