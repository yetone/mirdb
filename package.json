{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Homepage - Static website for MirDB persistent key-value store",
  "type": "module",
  "scripts": {
    "dev": "npx serve website -p 3000",
    "test": "npm run test:e2e",
    "test:e2e": "npx playwright test",
    "test:unit": "npx vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "serve": "^14.2.0",
    "vitest": "^1.3.0"
  }
}
