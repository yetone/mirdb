---
name: react-router-app-integration-test
description: Use when writing React Testing Library integration tests that need to verify route changes happen after clicking a link in a component, by rendering the App (or a wider component tree) inside MemoryRouter and asserting the destination route mounted. Pattern for the MirDB homepage CTA/navbar routing tests.
metadata:
  scope: project
---

See [README.md](references/README.md) for full documentation.
