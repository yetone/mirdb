---
name: mirdb-jsdom-viewport-testing
description: Test viewport-aware React components in Vitest + JSDOM. Use when writing or debugging tests for components whose behavior changes with window.innerWidth, when CSS-media-query-based hiding does not work in tests, or when you need to drive a synthetic resize through act() to exercise resize-listener code paths.
metadata:
  scope: project
---

See [README.md](references/README.md) for full documentation.
