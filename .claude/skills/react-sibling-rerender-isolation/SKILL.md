---
name: react-sibling-rerender-isolation
description: Pattern for testing React re-render isolation without coupling to `React.memo`. Use when you need to assert that a state change in one component does not cause an unrelated sibling component to re-render, and want the assertion to remain valid regardless of whether either component is memoised.
scope: organization
---

See [README.md](references/README.md) for full documentation.
