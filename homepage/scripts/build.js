/*
 * Owner: Scenario 11 - Performance & Asset Optimization.
 * Optional build step:
 *   - Inline CSS / minify HTML, CSS, JS
 *   - Compress images (architecture.svg should already be small)
 *   - Emit dist/index.html and dist/assets/*
 * Performance budget: total page weight < 200KB gzip; LCP < 2s on mid-tier broadband.
 */
