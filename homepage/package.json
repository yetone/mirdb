{
  "name": "mirdb-homepage-tests",
  "version": "1.0.0",
  "description": "Accessibility tests for MirDB Homepage",
  "scripts": {
    "test": "jest --verbose",
    "test:e2e": "jest --config=jest.e2e.config.js --verbose"
  },
  "devDependencies": {
    "@axe-core/cli": "^4.10.0",
    "axe-core": "^4.10.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.50.0"
  }
}
