{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Product Homepage - A high-performance persistent key-value store",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "@vitest/browser": "^1.0.0",
    "jsdom": "^24.0.0",
    "@testing-library/dom": "^10.0.0",
    "playwright": "^1.40.0",
    "@playwright/test": "^1.40.0"
  }
}
