{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Homepage - Public landing page for MirDB",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run tests/e2e",
    "lint": "echo 'Lint not configured'",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "@testing-library/dom": "^10.0.0",
    "playwright": "^1.44.0",
    "axe-core": "^4.9.0"
  },
  "private": true
}
