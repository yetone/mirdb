{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Homepage",
  "main": "index.html",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "npx serve . -p 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
