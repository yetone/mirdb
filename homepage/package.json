{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Homepage Testing Suite",
  "scripts": {
    "test": "npm run test:e2e",
    "test:unit": "jest tests/unit",
    "test:e2e": "playwright test tests/e2e",
    "serve": "cd _site && npx http-server -p 4000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
