{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB product homepage - a persistent key-value store with Memcached protocol compatibility",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@testing-library/dom": "^10.0.0",
    "linkedom": "^0.18.0"
  }
}
