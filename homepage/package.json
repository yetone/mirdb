{
  "name": "mirdb-homepage",
  "version": "1.0.0",
  "description": "MirDB Homepage - A persistent key-value store with Memcached protocol",
  "scripts": {
    "test": "npx jest --config jest.config.js",
    "test:unit": "npx jest tests/unit --config jest.config.js",
    "test:integration": "npx jest tests/integration --config jest.config.js",
    "test:e2e": "npx jest tests/e2e --config jest.config.js",
    "serve": "npx http-server . -p 8080",
    "build": "echo 'Static site - no build required'"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "@testing-library/dom": "^9.3.4",
    "@testing-library/jest-dom": "^6.4.2"
  },
  "keywords": ["mirdb", "key-value", "memcached", "persistent"],
  "license": "MIT"
}
