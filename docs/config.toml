baseURL = "https://yetone.github.io/mirdb/"
languageCode = "en-us"
title = "MirDB - Persistent Key-Value Store with Memcached Protocol"
# No theme - using custom layouts

[build]
  writeStats = true

[minify]
  minifyOutput = true

[params]
  description = "MirDB is a persistent key-value store with Memcached protocol support, built with Rust using LSM tree architecture."
  githubUrl = "https://github.com/yetone/mirdb"

[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = false
    noClasses = false
